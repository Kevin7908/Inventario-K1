name: inventario_k1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  path_provider: ^2.1.5
  get_it: ^9.2.1
  provider: ^6.1.1
  drift: ^2.32.1
  drift_flutter: ^0.3.0
  intl: ^0.20.2
  equatable: ^2.0.8
  file_picker: ^8.1.7
  bcrypt: ^1.2.0
  mailer: ^6.1.0
  shared_preferences: ^2.3.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.32.1
  build_runner: ^2.13.1

flutter:
  uses-material-design: true
